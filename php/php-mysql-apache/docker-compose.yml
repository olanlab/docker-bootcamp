version: "3"

services:
    php:
        build:
            context: ./php
        ports:
            - "8080:80"
        volumes:
            - ./site:/var/www/html/
    database:
        image: mysql:5.7
        environment:
            MYSQL_ROOT_PASSWORD: 1111

