events {}

http {

  include mime.types;

  server {
    listen 80;
    server_name localhost;

    set $key '1111';

    if ( $arg_key != $key ) {
      return 401 "Key is not available";
    }
    
    location /info {
       return 200 "$host | $uri | $args | $arg_name | $key";
    }

  }
}
