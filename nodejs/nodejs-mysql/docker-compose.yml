version: "3"
services: 
  db:
    build:
      dockerfile: Dockerfile
      context: ./db
    ports:
      - 33306:3306
  server:
    restart: always
    build:
      dockerfile: Dockerfile
      context: ./server
    environment:
      - DATABASE_HOST=db
    ports:
      - 8080:8080
    