docker run -d --name my-api-1 -p 3001:3000 -v $(pwd)/db1.json:/usr/app/db.json olanlab/json-server
docker run -d --name my-api-2 -p 3002:3000 -v $(pwd)/db2.json:/usr/app/db.json olanlab/json-server
docker run -d --name my-api-3 -p 3003:3000 -v $(pwd)/db3.json:/usr/app/db.json olanlab/json-server
docker run -d --name my-api-4 -p 3004:3000 -v $(pwd)/db4.json:/usr/app/db.json olanlab/json-server

// List all containers (only IDs)
docker ps -aq

// Stop all running containers
docker stop $(docker ps -aq)

// Remove all containers
docker rm $(docker ps -aq)
