sudo: required
services:
  - docker

before_install:
  - docker build -t olanlab/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run olanlab/react-test npm test -- --coverage

after_success:
  - docker build -t olanlab/multi-client ./client
  - docker build -t olanlab/multi-nginx ./nginx
  - docker build -t olanlab/multi-server ./server
  - docker build -t olanlab/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push olanlab/multi-client
  - docker push olanlab/multi-nginx
  - docker push olanlab/multi-server
  - docker push olanlab/multi-worker