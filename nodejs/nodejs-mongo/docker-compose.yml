version: "3"
services: 
  server:
    restart: always
    build:
      dockerfile: Dockerfile
      context: ./server
    ports:
      - 8080:8080
    environment:
      - DATABASE_HOST=host.docker.internal
#      - DATABASE_HOST=db
    